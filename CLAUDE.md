# CLAUDE.md

> このファイルはClaude Codeがプロジェクトを読み込む際に自動参照する設定ファイルです。
> コーディング時は常にこの内容を前提として実装してください。

---

## プロダクト概要

**web制作チーム向け タスク・案件・工数管理ツール**

web制作会社のディレクター・デザイナー・コーダーが使う社内SaaS。
複数プロジェクトが同時並行で進む中で「どの案件がどのフェーズか」「誰が何をやっているか」を
チーム全員がリアルタイムで把握できることが最大の目的。

### 解決する課題
- ディレクターの指示遅れ → 下流工程の遅延
- デザイナー・コーダーの着手遅延 → 納期ギリギリ納品・クレーム
- フェーズ別工数の見えない化 → 見積もり精度が低い

### スコープ外（実装しないもの）
- クライアントへの外部公開・クライアントログイン
- 請求書・見積書の生成
- Notion / Backlog 等の外部ツール連携
- スマートフォンネイティブアプリ（ブラウザ対応のみ）

---

## 技術スタック

### フロントエンド
| 技術 | バージョン | 用途 |
|---|---|---|
| Next.js | 15.x | App Router構成。SSR/ISRでパフォーマンス最適化 |
| TypeScript | 5.x | 全コンポーネントに適用。型安全な開発 |
| Tailwind CSS | 3.x | ユーティリティCSS |
| shadcn/ui | latest | UIコンポーネント |
| TanStack Query | 5.x | サーバー状態管理・キャッシュ・再取得 |
| zustand | latest | クライアント状態管理 |
| react-hook-form | latest | フォーム管理 |
| zod | latest | バリデーションスキーマ |
| Storybook | latest | コンポーネントドキュメント |
| Jest + React Testing Library | latest | ユニットテスト |
| Cypress | latest | E2Eテスト |

### バックエンド
| 技術 | バージョン | 用途 |
|---|---|---|
| Laravel | 11.x | REST API。FormRequestでバリデーション |
| PHP | 8.3 | 実行環境 |
| tymon/jwt-auth | 2.x | JWT認証 |
| Laravel Queues | — | AI処理の非同期実行（Redisドライバ） |

### インフラ・DB
| 技術 | 用途 |
|---|---|
| MySQL 8.0 | メインDB（AWS RDS） |
| Redis 7.x | APIキャッシュ・Queueドライバ（AWS ElastiCache） |
| AWS EC2 | Laravelアプリサーバー |
| AWS RDS | MySQL管理サービス |
| AWS ElastiCache | Redis管理サービス |
| AWS CloudFront | Next.js静的アセット配信・CDN |
| AWS S3 | ファイルストレージ（アバター画像等） |
| Docker | ローカル開発環境 |
| GitHub Actions | CI/CD（PRでテスト、mainでデプロイ） |

### 外部API
| サービス | 用途 |
|---|---|
| Claude API（claude-sonnet-4-6） | タスク分解・工数提案・優先度提案・リスク検知 |
| SendGrid | メンバー招待メール送信（Laravel Mail経由） |

---

## 認証仕様

- **方式**: JWT（tymon/jwt-auth）
- **アクセストークン有効期限**: 60分
- **リフレッシュトークン有効期限**: 30日
- **通信**: HTTPS（TLS 1.2以上）必須
- **CORS**: Next.jsドメインのみ許可

---

## ユーザー・権限

- **チーム規模**: 最大30人 / 1プロジェクト3〜6人
- **同時接続数**: 最大100セッション
- **ロール**: 現フェーズは全メンバー同一権限（区別なし）
  - 将来的にディレクターのみプロジェクト追加・ステータス変更に制限予定（Phase 3以降）

### 全メンバーが行えること
- プロジェクトの新規作成・編集・削除（論理削除）・ステータス変更
- タスクの追加・編集・削除（自分・他メンバーどちらも可）
- メンバー招待（SendGrid）・削除
- 工数の入力・閲覧（全メンバー分）
- 個人タスクの管理（自分のみ閲覧・編集）
- スケジュール（ガントチャート）の閲覧

---

> DB設計（ER図・テーブル定義）は [`docs/db-design.md`](docs/db-design.md) および [`docs/tables/`](docs/tables/) を参照。
> API設計は [`docs/api-design.md`](docs/api-design.md) を参照。

---

## 機能要件サマリー

### Phase 1（必須・最優先）
- プロジェクトCRUD（名前・クライアント名・開始日・納品日・見積工数・担当メンバー）
- フェーズ管理（要件定義→WF→デザイン→コーディング→テスト→納品）
- **ガントチャート表示**（`/schedule`）← 最重要画面。横軸:日付、縦軸:プロジェクト、今日線あり
- 進捗率自動算出（完了タスク数÷全タスク数）
- タスクCRUD（優先度・種別タグ・見積工数・担当者・期日・メモ）
- タスクステータス管理（未着手→進行中→レビュー待ち→完了）
  - チェックボックスON → 取り消し線 + 「完了」へ自動遷移
  - 完了タスク削除 → 物理削除（forceDelete）
- サブタスク（1階層のみ）
- チェックリスト + フェーズ別プリセットテンプレート適用
- メンバー招待（SendGrid）・一覧・削除
- JWT認証（ログイン・ログアウト・リフレッシュ・パスワードリセット）

### Phase 2
- 工数入力（0.5h単位）・作業ログ記録（開始/終了）
- 工数集計（案件別・メンバー別・フェーズ別）・超過アラート
- マイページ（個人タスク + 案件タスクの横断ビュー。今日/今週切替）
- 工数レポート画面（月別・メンバー別・案件別）

### Phase 3（AI機能）
- タスク自動分解（Claude API → Laravelキューで非同期 → UI通知）
- 見積もり工数提案
- 今日のタスク優先度提案（マイページ）
- リスクサマリー（ダッシュボード）

---

## 画面一覧

| 画面名 | URLパス | Phase |
|---|---|---|
| ダッシュボード | `/dashboard` | 1 |
| スケジュール（ガントチャート） | `/schedule` | 1 ★重要 |
| プロジェクト一覧 | `/projects` | 1 |
| プロジェクト詳細 | `/projects/[id]` | 1 |
| タスク詳細 | `/projects/[id]/tasks/[id]` | 1 |
| マイページ | `/my` | 2 |
| 工数レポート | `/report` | 2 |
| チーム設定 | `/settings/team` | 1 |
| ログイン | `/login` | 1 |
| 招待受諾・登録 | `/invite/[token]` | 1 |
| テンプレート管理 | `/settings/templates` | 3 |

---

## 非機能要件

| 項目 | 要件 |
|---|---|
| 応答速度 | 通常操作200ms以内。AI処理は非同期（Queue） |
| 同時接続数 | 最大100セッション |
| キャッシュ | RedisでAPIレスポンスキャッシュ。重い集計はバッチ更新 |
| 稼働率 | 月次99%以上 |
| バックアップ | RDS自動バックアップ（日次、7日間保持） |
| ブラウザ | Chrome最新版 / Safari最新版 |
| デバイス | Windows / Mac / iOS / Android（ブラウザ経由） |
| ログ | AWS CloudWatch |
| テスト | PHPUnit（Laravel API）/ Jest（フロントエンド）/ Cypress（E2E） |

---

## コーディング規約

### 共通
- コメントは日本語で書く
- 変数名・関数名は英語（キャメルケース or スネークケース）
- 環境変数は `.env` で管理。ハードコード禁止

### Laravel（バックエンド）
- バリデーション: `FormRequest` クラスを必ず使う
- 権限制御: `Policy` クラスで管理
- APIレスポンス: `Resource` クラスで整形
- ルーティング: `routes/api.php` にまとめる
- 削除: プロジェクトは `SoftDeletes`、完了済みタスクは `forceDelete()`
- AI処理: `Job` クラスで非同期化（Redisキュー）

### Next.js（フロントエンド）
- ディレクトリ: App Router（`app/`）を使う
- データ取得: TanStack Query でサーバー状態を管理
- フォーム: react-hook-form + zod でバリデーション
- スタイル: Tailwind CSS + shadcn/ui
- API通信: `apiFetch` / `publicFetch` のラッパー関数を経由する（直接 fetch しない）

---

## ガントチャートの実装メモ

現行の工程表（`参考_現行工程表`）をデジタル化したもの。以下の構造を参考にすること。

```
工程番号 | 作業名 | 担当者 | ステータス | 期間(日数) | 開始日 | 終了日
例:
1-1 | キックオフ      | 両社 | 完了 | 1日
1-2 | 素材準備        | 貴社 | 完了 | 7日
3-8 | ワイヤーフレーム | AIC  | 完了 | 7日
4-1 | トップデザイン   | AIC  | 完了 | 4日
6-1 | テストサイト構築 | AIC  | 完了 | 28日
7-1 | 公開作業        | AIC  | 完了 | 1日
```

- 横軸: 日付（日単位）
- 縦軸: プロジェクト → フェーズ
- 今日の位置を縦線で表示
- フェーズバーの色: ステータスで色分け（未着手=グレー、進行中=青、完了=緑）
- 複数プロジェクトの納期重複を一覧で把握できること

